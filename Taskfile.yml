version: '3'

vars:
  BIN_DIR: bin
  CMD_PATH: ./cmd/todushka

tasks:
  default:
    desc: List all available tasks
    cmds:
      - task --list-all

  test:
    desc: Run all unit + property tests
    cmds:
      - go test ./...

  test-race:
    desc: Run all tests with race detector
    cmds:
      - go test -race ./...

  build:
    desc: Build todushka binary for current host
    cmds:
      - mkdir -p {{.BIN_DIR}}
      - go build -o {{.BIN_DIR}}/todushka {{.CMD_PATH}}

  lint:
    desc: Run golangci-lint
    cmds:
      - golangci-lint run

  fmt:
    desc: Format Go source
    cmds:
      - go fmt ./...
      - goimports -w .

  tidy:
    desc: Tidy go.mod / go.sum
    cmds:
      - go mod tidy

  run:
    desc: Build and run todushka
    cmds:
      - task: build
      - "{{.BIN_DIR}}/todushka {{.CLI_ARGS}}"

  cross-compile:
    desc: Cross-compile for linux/darwin × amd64/arm64
    cmds:
      - mkdir -p {{.BIN_DIR}}
      - GOOS=linux   GOARCH=amd64 go build -o {{.BIN_DIR}}/todushka-linux-amd64  {{.CMD_PATH}}
      - GOOS=linux   GOARCH=arm64 go build -o {{.BIN_DIR}}/todushka-linux-arm64  {{.CMD_PATH}}
      - GOOS=darwin  GOARCH=amd64 go build -o {{.BIN_DIR}}/todushka-darwin-amd64 {{.CMD_PATH}}
      - GOOS=darwin  GOARCH=arm64 go build -o {{.BIN_DIR}}/todushka-darwin-arm64 {{.CMD_PATH}}
